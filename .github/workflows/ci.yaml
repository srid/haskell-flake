name: "CI"
on:
  push:
    branches:
      - master
  pull_request:
jobs:
  configure:
    runs-on: self-hosted
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
     - name: Checkout to repository
       uses: actions/checkout@v4
     - name: Set matrix data
       id: set-matrix
       run: |
         nix --refresh run github:srid/nixci/matrix -- dump-github-actions-matrix --systems=aarch64-linux,aarch64-darwin > ./matrix.json
         cat ./matrix.json | jq -c .
         echo "matrix=$(jq -c . < ./matrix.json)" >> $GITHUB_OUTPUT
  nix:
    runs-on: self-hosted
    needs: configure
    strategy:
      matrix: ${{ fromJson(needs.configure.outputs.matrix) }}
    steps:
      - uses: actions/checkout@v4
      - run: nix --refresh run github:srid/nixci/matrix -- build --build-systems "github:nix-systems/${{ matrix.build-system }}" .#default.${{ matrix.subflake}}
