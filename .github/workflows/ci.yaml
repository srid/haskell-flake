name: "CI"
on:
  push:
    branches:
      - master
  pull_request:
jobs:
  checks:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Build Nix
        id: build-nix
        run: |
          nixci
      - name: Run test
        id: test
        run: |
          nix run nixpkgs#bash runtest.sh
